# Claude-Review-Mode
An iteration to existing Claude to help users evaluate outputs. It is an evaluation layer that surfaces locked-in signals as three actionable artifacts - keeping the user in control of final judgment. 
